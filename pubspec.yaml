name: book_app
description: "Recipe Book with Hive"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # HIVE
  hive: ^2.2.3
  hive_flutter: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  # CODE GENERATION
  build_runner: ^2.4.6
  hive_generator: ^2.0.1

flutter:
  uses-material-design: true